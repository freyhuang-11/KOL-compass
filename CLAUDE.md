# KOL Compass — 工作规矩（Claude 必读）

## 决策必须走 Plan 模式（最高优先）
**每一次需要用户做判断 / 拍板的事，都必须用 plan 模式提交，不要用普通文字零散提问。**
做法：`EnterPlanMode` → 把所有待决策项用 `AskUserQuestion`（结构化选项）一次性给用户 → 用户答完后写进 plan 文件 → `ExitPlanMode` 让用户过目批准。
- 适用：方案选择、范围划定、改动方向、外部条件确认（如要不要某 API/key）、任何"只有用户能定"的事。
- 不适用：用户已明确下达的执行指令、纯事实汇报、可自行决定的默认项。

## 沟通铁律
- 先讨论再操作；给**结论 + 判断**，不要甩"A/B/C 选项"让用户挑。
- **要事实不要假设**：聊 TikTok API 能力前必查文档；拿不准的不假装、不造假（宁可说"卡在 API、需外部条件"）。
- 跟老系统 `D:\tiktok-creator-tool` 对比要克制（Compass 是对外 SaaS，多租户/API 驱动，规则不同）。
- mental model：当前版本做**单商家多店铺**；服务商（多商家）形态推迟到上线后版本。

## 交付后验收汇报
- 每一轮改完，明确列「**要验收什么** + **分别做了什么**」，一一对应。
- 未验收的累积，下一轮做完时把历次未验收点一起给用户。
- 验收台账维护在 `docs/CLAUDE_ACCEPTANCE.md`（每轮一节，状态 ⏳未验收 / ✅已验收）。

## 不碰
- 不动 `D:\tiktok-creator-tool`；不占用 / 不关闭端口 `5173` / `5174`。
- 本项目端口：前端 `5175`、后端 `8015`。
- 真实 TikTok 私信/邀约发送受 sandbox 配额限制，不假装成功。

## WhatsApp 边界（已修正）
- WhatsApp **可作达人联系方式展示/录入/导入**（KOL详情、编辑、CSV）——让 BD 自己决定要不要用 WA 联系。
- 但 **WA 不作系统发送渠道**：不进建联渠道选择、消息模板渠道、平台发送开关。

## 前端缓存（必做，否则改了不生效）
- 每次改 `app.js` 或 `app.css`，**必须同时 bump `index.html` 里的 `?v=` 版本号**，否则用户普通刷新拿的是缓存旧代码（2026-06-24 就因为版本号没改，R1–R5 改动一直没加载，被当成"没生效"）。

## 启动 / 自启
- 前端：`python -m http.server 5175 --bind 127.0.0.1`；后端：`node server.js`（8015）。
- 已配静默开机自启：启动文件夹 `KOL_Compass_Autostart.vbs` → `start-silent.vbs`（无窗口）；看门狗计划任务 `KOL Compass Watchdog`（每 3 分钟检查，掉了静默拉回）。
- 从助手会话内起的进程会随会话结束被回收；要常驻用上面的自启/看门狗，或让用户跑 `start-full.bat`。
