# Claude 接手后的验收台账

> 规则：每一轮改完都列「要验收什么 + 分别做了什么」。未验收的累积，等用户验收后标记 ✅。

## 建联后闭环 R1 — 北极星漏斗看板　v=20260702-r44　【状态：⏳ 未验收】

完成时间：2026-07-02。用户选"建联后闭环(本地能做的部分)"。控制台顶部加**北极星漏斗**(用户定：三个率按**达人数去重**算)。

| 要验收 | 做了什么 | 验证 |
|---|---|---|
| 漏斗 6 阶 | 触达→回复→寄样→产出→出单→复投，各阶按 `creatorId` 去重计数，可点跳对应页 | ✅ 真机渲染 6 阶全在 |
| 回复率 | 回复达人 ÷ 触达达人（回复=outreach 状态"待我方回复/已回复/洽谈中/合作中/已成交"）| ✅ 显示 0%(0/0)，新店无数据正常 |
| 样品成功率 | 产出达人 ÷ 寄样达人（产出=合作有视频或直播）| ✅ |
| 成交率 | 出单达人 ÷ 触达达人（出单=合作有 GMV/订单）| ✅ 旁标"联盟订单API待接,暂按合作台账" |

诚实边界：①**成交率的自动化要接 Affiliate Order API**——docs 确认有 Orders 分组，但确切端点 path 现从 JS 渲染 docs/搜索都取不到(登录墙)，且真实店 jasarielpicks 是新店零联盟订单。**建议 Codex 在 partner center 取端点**；现暂用合作台账 GMV 真算(非占位)。②新店数据空 → 各阶现为 0，管道已通，数字随 BD 操作填。③其余建联后闭环(入站收消息→回复率闭环、投流决策、复投池深化)待续。

## 采集器收尾 — 换词循环采全量 + 头像/新指标 + UI 补齐　v=20260702-r43　【状态：⏳ 未验收】

完成时间：2026-07-02。承接口轮询版，把 SG 采满并补齐达人信息。

| 要验收 | 做了什么 | 验证 |
|---|---|---|
| 突破推荐池采全量 | `market-inject` 内置 60 类目/品类词表，全自动换词深翻（每词≤60页），推荐池外达人 0 重合 | ✅ 库 3448→**17593+** 且在涨；4 词各 40 全不重复 |
| 刷新不重扫 | 进度存 localStorage(`__kcm_qi`)，F5 从上次的词接着采 | ✅ |
| 头像修复 | 采集器把 `avatar` 当字符串取空了 → 改取 `avatar.url_list[0]` | ✅ 新采 209 个都带头像 |
| 新指标入库 | 采集器采「带货件数/视频互动/是否合作过/粉丝top年龄·性别」，后端 ingest-marketplace 存住 | ✅ 209 个有 unitsSold |
| **新指标显示到达人库 UI** | 详情「建联前评估」卡加「达人广场数据」块：带货件数/视频互动/主要粉丝性别(男女)/年龄 + 「曾与本店合作」标 | ✅ 真机 5175：57带货·175互动·男·55+·曾合作，零报错 |

| **头像不显示（灰圈）修复** | 根因≠没采到：`serveCreatorAvatar` 裸 `fetch(url)` 拉 TikTok CDN **无 headers 无超时→永久挂起**(实测 HTTP 000/8s)。加 **UA+Referer 过防盗链 + 6s 超时** | ✅ 实测 `/api/avatar?cid=xiaowang626` **HTTP 200·image/webp·6468字节**（原来挂死）|

诚实边界：①存量 ~15400 个（修复前采的）无头像 URL/新指标，靠换词循环重采慢慢补（用户定：不做一次性回扫）——有头像 URL 的现在能正常显示了。②达人库列表数（如"3695"）是前端 `state.creators` **旧快照**，采集器一直在涨库，**刷新页面即拉到最新 17183**（非 bug）。③门控字段拿不到。

## 采集器重写 — 达人库采集器改「接口轮询」　【状态：✅ 用户已验收】

完成时间：2026-07-01。用户反复反映采集器"不翻页/卡死/采不满"。真机排查后**从"滚 DOM"整个重写成"直接调 marketplace/find 接口轮询"**。

| 要验收 | 做了什么 | 验证 |
|---|---|---|
| 不再卡死 | 采集器**不碰页面 DOM**，MAIN 世界拿页面已签名的 find URL(`performance` 里取)直接 POST 翻页 | ✅ 页面纹丝不动、不冻 |
| 采得到 / 会翻页 | 每 3.5s 调一次接口翻页(page+search_key+item_cursor)；深翻 18 页 360 个全不重复、`has_more` 仍 true | ✅ 库 3348→3424，本会话净增 +63 |
| code 100000 报错 | 根因：钩子在 TikTok 签名层内侧抓到**签名前** URL → 改从 `performance` 取**已签名**的实际请求 URL | ✅ 实测 5/5 code 0 |
| 面板诚实计数 | 从"会话去重数"(会误导)改成**后端库总数 + 本会话净增入库**为准 | ✅ 面板显示"达人库总数 3424 / 净增 +63" |

诚实边界：① 我一度误判"卡 3355 到顶"，实为深翻未尽、**没到顶**(用户纠正,已改口)。② 想采全量仍需**类目筛选**突破推荐轮换——待用户在页面手动勾一次类目、我抓请求拿字段。改动全在 `harvester/`(不发 B 端)。

## Phase 1.6 — 扩展弹窗：采集明细 + 红绿灯 + 综合结论　v=20260629-r28　【状态：⏳ 未验收】

完成时间：2026-06-29。用户要"采集信息的体现 + 红绿灯明显提示"。

| 要验收 | 做了什么 | 验证 |
|---|---|---|
| 采集明细 | 弹窗清列：达人/粉丝、近期表现(均播/播放区间/均评论)、内容话题标签 | 代码就绪 |
| 红绿灯 | 4 个信号各 🟢🟡🔴：**内容契合**(达人在发的 vs 你商品品类)、互动率、买家意向、发布活跃 | ✅ 契合逻辑 python 验通(健康达人遇健康商家🟢/美妆🔴) |
| 综合结论 | 顶部大横幅：🟢建议建联 / 🟡可考虑 / 🔴谨慎(品类不符直接谨慎) | 代码就绪 |
| 契合上下文 | 后端 `GET/POST /api/merchant/context`(当前店铺品类)；前端同步商品时自动推送；扩展取来算契合 | ✅ 端点 UTF-8 round-trip 验通 |

诚实边界：弹窗 UI 需你**重载扩展(chrome://extensions刷新)+开达人页**眼验；后端+契合逻辑已验。契合需先在 Compass 同步过商品(推过品类)；评论意向需打开几条视频。

## Phase 1.5 — 评论买家意向（建联前齐活）　v=20260629-r27　【状态：⏳ 未验收】

完成时间：2026-06-29。补上用户早就要的"评论里有没有匹配的购买意向"——建联前最后一块。**至此建联前所有内容跑通**。

| 要验收 | 做了什么 | 验证 |
|---|---|---|
| 扩展钩评论 | `inject.js` 加钩 `/api/comment/list`→emit评论；`content.js` 买家意向分析(中/英/SEA关键词:哪买/链接/多少钱/where to buy/link/price)→intentRatio | mock 验通 |
| 评分 | 潜力分加「购买意向」维度(w0.18，intentRatio×5封顶，20%评论带意向=满分)，仅有数据时计入(非破坏) | ✅ Chrome:购买意向75(15%×5) |
| 详情卡 | 「建联前评估」显示评论买家意向%+命中数+样例评论 | ✅ "15%(6/40)·where to buy this??·link pls·多少钱" |
| popup | 显示评论意向；无则提示打开视频让评论加载 | 代码就绪 |

**建联前完整清单（一起验收）**：①出单潜力分(5维:出单/类目契合/内容触达/建联效率/购买意向+履约) ②内容契合(扩展) ③近期视频表现(钩item_list) ④评论买家意向(钩comment) ⑤排除规则(已建联/冷却/黑名单/不感兴趣)。

诚实边界：①评论意向要 BD 在达人页**打开几条视频**让评论加载才钩得到(评论不在主页墙上)——这是被动钩的固有限制，popup有提示。②扩展真机抓取需你重载扩展(chrome://extensions刷新)+浏览达人页。数据管道+评分+UI 已用真实mock端到端验。

## Phase 2 — 建联后·样品成功率（官方寄样API + 风险清单）　v=20260629-r26　【状态：⏳ 未验收】

完成时间：2026-06-29。**全程官方 API、零手填**（符合约束）。竞品全量验证后确认建联后是护城河，达人精灵走爬+邮件碰不到店铺真实寄样数据。

| 要验收 | 做了什么 | 验证 |
|---|---|---|
| API 端点(实地查证) | 官方 docs 渲染读到确切 path：`POST /affiliate_seller/202508/sample_applications/search`、`/202409/sample_applications/{id}/fulfillments/search`、内容详情/Orders/达人表现 | ✅ 文档当面确认 |
| 后端 | `searchSampleApplications`/`searchSampleFulfillments`/`sampleOverview`(按cipher取token)+`POST /api/tiktok/samples`；自动算"拿样未产出=骗样嫌疑"风险旗标 | ✅ **真实店实测有真数据**：Goosepicks 申请 Xrock薄荷糖、状态/履约率/佣金全字段 |
| 寄样管理页 | 改成拉官方真实数据：达人(头像/粉丝)/商品/状态(中文)/佣金/履约率/内容产出/风险；状态码→中文(REJECT_CANCELLED→已取消) | ✅ Chrome 真机渲染真实那条、零报错 |
| 风险清单 | 自动标(拿样未产出/低履约<30%)+BD一键「标记风险」(骗样/不履约/沟通差)；切店重载 | ✅ 渲染就位(当前那条已取消、未触发风险，正确) |

诚实边界：①新店 jasarielpicks 目前只 1 条已取消申请，"拿样未产出"风险逻辑要真有"寄出未产出"数据才看得到效果(逻辑已就位)。②内容验收(Creator Content Detail)+出单归因(Orders)=Phase 3。③承诺不做(无API)。④样品风控信号目前用履约率+风险旗标，后续可结合更多历史。

## Phase 1 — 建联前体检（扩展钩 item_list）　v=20260629-r25　【状态：⏳ 未验收】

完成时间：2026-06-29。主线计划见 `plans/mellow-zooming-puzzle.md`。把扩展从"抓DOM"重做成"钩 TikTok item_list 接口"——拿 API 给不了的近期内容表现。

| 要验收 | 做了什么 | 验证 |
|---|---|---|
| 扩展架构 | `extension/inject.js`(MAIN世界,document_start 钩 fetch/XHR 捕获 `/api/post|repost/item_list`)→postMessage；`content.js`(隔离世界)聚合近15视频均播/赞/评论/互动率+范围+发布频率+话题词云 | 钩取方法前已实证(@xianexy 抓到 playCount) |
| 后端 | `ingestExtensionCreators` 接收并清洗 `recentPerf`(均播/互动率/发布频率/播放区间/最近发布) | ✅ ingest 测通、入库正确 |
| 评分+详情 | 潜力分"内容触达"自动用真实近期均播；详情「建联前评估」卡新增近期表现4格(均播/互动率/条周/均评论)+播放区间 | ✅ Chrome 真机：内容触达83、卡显示"4.2万均播·4.6%互动·5.2条/周·3千~21万" |
| popup | 显示近期表现；没钩到视频数据不让采(避免跟API重复) | 代码就绪 |

诚实边界：①扩展在真实 TikTok 页钩 item_list 需**你重载扩展(chrome://extensions→刷新)+浏览达人主页**实测——我测不了你登录态；数据管道+UI 已用真实 mock 端到端验过。②评论买家意向(钩 comment 接口)= Phase 1.5 后置。③潜力分要加载店铺商品才有"类目契合"，否则偏低(正常)。

## Round 21 — 建联页商品排序 + 扩展分发澄清（v=20260629-r24）　【状态：⏳ 未验收】

完成时间：2026-06-29。

| # | 问题 | 做了什么 | 验证 |
|---|---|---|---|
| 1 | 发起建联页：可用商品前置、不可用沉底 | `productMultiPicker` + `productPicker` 都按 `productPromotable` 排序（可用在前） | ✅ Chrome 验证：唯一可用商品置顶，下架全沉底 |
| 2 | 扩展是否上 Google 商店 | （仅澄清，无代码）确认最终给 B 端是上 Chrome 应用商店，但属**上线动作**：需开发者账号($5)+隐私政策+商店审核(1-3天)+**后端先部署公网**(扩展不能连 localhost)。节奏=现在本地验证、要给客户用时再上架（和"部署上公网"绑定）。可先备好隐私政策+listing文案待上线用 | — |

## Round 20 — 商品筛选 + 建联流程 + 达人详情（v=r23，已浏览器验证）　【状态：⏳ 未验收】

完成时间：2026-06-28。

| # | 问题 | 做了什么 | 浏览器验证 |
|---|---|---|---|
| 1 | 商品页加可用/不可用筛选 | 商品搜索旁加 **全部/可用/不可用** 下拉（按 `productPromotable` 过滤） | ✅ 下拉三项在 |
| 2 | 商品「建联/合作」点了进空记录页 | **建联流程从达人库发起**：商品「建联」→ 带商品上下文进达人库（顶部横幅"正在为商品X物色达人"，一键建联自动选中该商品）；下架商品「建联」置灰；「合作」→ 改名「合作记录」(查看，不是操作) | ✅ 按钮=详情/合作记录/建联(下架置灰) |
| 3 | 达人 detail_url 补回 | 详情联系方式 + 基本信息加 **TikTok 主页链接**（`creatorProfileUrl`，优先真 url，否则 username 构造），B 端可自行核对 | ✅ "@xxx ↗"/"打开↗" |
| 4 | 详情底部内部备注 + 假标签 | 删掉底部「标签备注」内部文字卡；**三个标签做成真可点页**：沟通记录/合作记录/基本信息（基本信息=达人完整资料表+内容信号） | ✅ 切「基本信息」显示完整资料表 |
| 5 | 扩展不会用 | （仅解释）扩展代码已做好，但需你手动 `chrome://extensions` 加载 + 真实 TikTok 页实测；不急、不影响其它；要用时我教学模式带你点 | — |

✅ **Chrome 真机验证零报错**：详情页 TikTok 链接、无内部备注卡、三标签可点切换、基本信息资料表、商品筛选、建联按钮置灰全部确认。

## Round 19 — 内容契合度评分（扩展数据接入潜力分，自主执行）　【状态：⏳ 未验收】

完成时间：2026-06-28　v=20260628-r21。把 R18 扩展采集的内容信号变成"契合你这个商品"的评分——建联前核心差异化落地。

| 要验收 | 做了什么 |
|---|---|
| 内容实证契合 | `creatorCategoryFitScore` 升级：达人**近期内容实际涉及的品类**(扩展采集 topics + bio/文案关键词扫描，`TOPIC_TO_CATEGORY` 英中映射)对上商家商品品类 → **直接满分**。**非破坏**：没扩展数据的达人评分完全不变，只多开一条"内容实证→满分"的路 |
| 详情卡内容信号 | 「建联前评估」卡加「内容信号」区：内容品类标签(命中商家品类的标绿)+简介+近期内容条数 |
| 达人库标记 | 有内容数据的达人列表显示「内容✓」徽标，BD 一眼看出谁有更强信号 |

验证：抽出契合逻辑跑真值 ✅——内容命中商家品类→100(即使无声明类目)；不相关→落回声明类目；无数据→25(不变)；无商品→null(跳过)。

诚实边界：①本轮纯前端(app.js)，逻辑已实测，但完整效果要扩展真采到内容后才看得到(需用户加载扩展+浏览真实达人页)；②`TOPIC_TO_CATEGORY` 映射目前覆盖主流品类，可按实际数据扩充。

## Round 18 — 主线：Chrome 扩展「建联前采集器」从零搭建（自主执行）　【状态：⏳ 未验收】

完成时间：2026-06-28。回主线做两根柱子里「建联前」的核心差异化：扩展采集 TikTok 达人内容信号(API 拿不到的)，并入达人库。

**为什么是它**：1B 出单归因当前双重受阻(新店零联盟订单+订单API端点docs JS渲染拿不到需实测)，没数据可验；扩展不靠订单/API/限流，直接读 BD 已登录 TikTok 前端，是 STRATEGY 里的 Wave 2 核心刀。

| 交付物 | 说明 | 验证 |
|---|---|---|
| 后端 ingest | `server.js` `ingestExtensionCreators` + `POST /api/creators/ingest`，按 username 并入库(librarySource=extension)，联系方式不被覆盖 | ✅ mock 测通：received1/changed1，bio/recentCaptions/contentTopics 入库、region 归一 |
| 扩展 `extension/` | MV3：`manifest.json` + `content.js`(TikTok主页 data-e2e 抽取粉丝/简介/近期文案/主题，限速降级) + `background.js`(POST) + `popup`(采集面板/Compass地址/自动采集开关) + `README.md` | ⏳ 需在 Chrome 加载未打包扩展、开真实 TikTok 达人页眼验(我没有用户登录态浏览器) |

诚实边界：①扩展的 DOM 抽取依赖 TikTok 选择器、改版需维护(已留降级)；②未做：**内容主题 vs 商家商品类目的契合度评分**(把潜力分从"通用数据"升级成"契合你这个商品")——这会改评分模型，留给用户拍板后做；③我无法在用户的登录态浏览器里真实验证抓取，只验了 ingest 管道。加载方式见 `extension/README.md`。

## Round 17 — 达人库可读性（用户离线4h自主执行）　【状态：⏳ 未验收】

完成时间：2026-06-28　v=20260628-r20。

| 要验收 | 做了什么 |
|---|---|
| 数字面板简化 | 「真实/补充/全部」内部分层 → 简化为「当前市场达人 N 位 + 可联系(有Email/WhatsApp) M 位」，藏掉内部口径 |
| GMV 官方口径 | GMV 列改 **10K/200K/1.5M**（官方 K/M），不再用"20万"。`gmvCompact()` |
| 内容表现拆分 | 原"视频X·直播Y"一列拆成 **「视频均播」「直播均观」两列**，各自独立数字 |
| 回复率 undefined | legacy 没回复率时显示 **"-"**，不再露 `undefined`。`creatorReplyRateDisplay()` |
| 筛选补充 | GMV 筛选加 **<10K/10K-100K/100K-1M/>1M** 档；类目筛选含「其他」(R16) |
| 其他类目 | 官方27类之外(voucher等)归「其他」并加回筛选项(本轮前半) |

补充(v=r22)：GMV 带货币前缀的也压成 K（`USD 104,486`→`USD 104.5K`），跟老库 200K/10K 统一。

**✅ 浏览器实测(Chrome 真机 127.0.0.1:5175)**：用 Claude-in-Chrome 加载达人库页，**零控制台报错**，确认：数字面板已简化、GMV 全 K 口径、内容表现拆「视频均播/直播均观」两列、回复率显示「-」、类目筛选干净且含「其他」、多店铺下拉两家、潜力分+高潜标正常、分页 79 页。R17/R19 前端渲染验证通过（R19 内容信号块需扩展真采到数据后才显示）。

## Round 16 — 多店铺验证通过 + 4 项跟进（v=20260628-r18）　【状态：⏳ 未验收】

完成时间：2026-06-28。**多店铺端到端打通**：store 现含两家(Sggoodthingsg/HydroSkin Lab + jasarielpicks)，授权日志确认 exchange OK。

| # | 问题 | 做了什么 |
|---|---|---|
| 0 | 之前授权老失败 | 根因：Compass 后端老挂(会话起的 node 被回收、看门狗没跟上)，回调打到空端口。已用常驻方式起后端。**⚠️遗留：后端进程稳定性靠看门狗，需确认看门狗真能拉起(见诚实边界)** |
| 1 | 授权成功、两店并存 | 多店铺已验证通过 ✅ |
| 2 | 授权后回到平台设置页(B端不该看到) | 后端回调跳转 `#admin`→**`#products`(产品管理页)** |
| 3 | 商品列表过多 | 产品管理加**分页**(20/50/100 条/页)，`paginationBar` 通用化(单位/翻页函数可配) |
| 4 | 下架商品也能建联 | 根因：`productPromotable` 只认中文"下架"，但状态已是英文码 `SELLER_DEACTIVATED`。改成按 `productStatusLabel` 判断，下架/删除/冻结/草稿/审核一律不可推广 |

诚实边界：①后端进程稳定性是真问题——会话内起的 node 跨回合会被回收，靠 `KOL Compass Watchdog`(每3分钟)拉起；需确认看门狗/autostart 真的在跑(之前进程列表里一度一个 Compass 后端都没有)。长期应让后端作为常驻服务。②前端样式仍需刷新 5175(r18)眼验。

## Round 15 — 实测反馈 8 项（多店铺并存 + UI/数据修复）　【状态：⏳ 未验收】

完成时间：2026-06-28　｜　验证：`node --check` server/app 通过；后端实测：health 列卖家、shops 返回并集+owner、/api/avatar 200 图片+二次命中缓存。版本 `v=20260628-r16`。

| # | 要验收的位置 | 做了什么 |
|---|---|---|
| 1 | **多店铺并存** | 根因：token 单文件覆盖式，授权第二家店把第一家覆盖了。改成**多 token 存储**(`.data/tiktok-tokens.json` 按 open_id 存多份)+ 迁移旧单文件 + `accessTokenForCipher`(按店取对 token) + `allAuthorizedShops`(并集)；shop-scoped 调用(商品/达人/类目/邀约/会话)全部按 cipher 取对应店 token；`/api/tiktok/shops` 返并集并回填；前端 `switchTikTokShop` 切店即静默重拉该店商品。**注意：jasarielpicks 的 token 已被覆盖丢失，需用户重新授权它，两店才并存。** |
| 2 | 授权成功页 | 后端回调换成干净成功页（✓ + 店铺名 + 返回按钮 + 自动跳转），不再吐 JSON/token |
| 3 | 达人类目 | 类目筛选**只用官方 L1**(27 项 canonical)，不再把达人脏类目值塞进选项；新增同义词归一(保健品→健康保健、3C→手机数码、居家用品→家居日用…)+ 拼接串(逗号/分号)拆分 |
| 4 | 出单潜力分逻辑 | （仅口头解释，未改）GMV35%+类目契合25%+触达20%+回复率20%(+履约15%)；诚实短板：重 GMV、老库回复率为0，现为粗排序 |
| 5 | 达人头像 | 平台达人有 TikTok 签名头像但会过期/防盗链裂图 → 新增 `/api/avatar?cid=` **后端拉图缓存到本地再服务**；前端头像走该端点，裂图回落首字母；老库达人无头像→首字母 |
| 6 | "建联过"标签 | 移除达人库行 + 详情卡里的历史建联徽标（那是另一套系统状态，数据留库不展示） |
| 7 | 列太长 | 粉丝/GMV/内容表现改**紧凑缩写**(5万/20万、"视频 1.2万 · 直播 800" 一行) |
| 8 | 商品状态错位 | 原始码 `SELLER_DEACTIVATED`→「卖家下架」等中文映射 + 状态胶囊；商品名 2 行截断；操作列不换行、修列宽对齐 |

诚实边界：①浏览器可视化仍靠后端实测+逻辑验证（预览工具撞 hermes 的 3000 端口）；前端样式需刷新 5175 眼验。②**多店铺要你重新授权 jasarielpicks** 才能看到两家并存（它 token 被覆盖丢了）。③老库达人在 TikTok 抓到前无头像。④潜力分权重未调（#4 只解释）。

## Round 14 — 导入老库「联系方式 + 建联历史」解锁真数据　【状态：⏳ 未验收】

完成时间：2026-06-28　｜　验证：`node --check` server.js/app.js 通过；导入端点返回 changed=1447；API 抽查字段正确；潜力分在 1433 真达人上分布合理(7–70, 均49)。版本号 `v=20260628-r14`。

> 背景：真实店是新店、被 TikTok 限流，达人库近空，两根柱子没数据可验。老系统 `tiktok_creators.db` 有 2647 个 SG 真达人——**只取联系方式(真值，TikTok 永远拿不到) + 建联历史**，指标按用户拍板「导入但明标估算/待刷新」，让 1A 评分立刻能验。

| 要验收的位置 | 做了什么 |
|---|---|
| 达人库（数据） | 一次性导入老库 **1447 个 SG 真达人**（email 1368 / whatsapp 846），达人库总数 →1918。指标标 `metricsEstimated`，联系方式/建联历史为真值 |
| 达人库表格 | 估算指标加「估」上标；状态列加**历史建联徽标**（曾洽谈·有意向 / 曾合作 / 曾拒绝 / 曾建联），让 BD 优先跟有意向、不重复骚扰已拒 |
| 达人潜力分 | 老库指标进来后**潜力分能算出来并排序**（1A 真实数据验证）；估算达人潜力分加「估」标 |
| 达人详情 | 新增**「建联前评估」卡**：潜力分拆解条 + 历史建联结果 + 「指标为估算，TikTok 同步后刷新」说明 |
| 后端合并保护 | `upsertPlatformCreators` 改非对称合并：**新鲜 TikTok 指标永远赢并清估算标**；老库估算**不反向覆盖**已有指标；联系方式/建联历史**空值不抹真值**（双向保护） |
| 导出脚本 | `scripts/export-legacy-creators.py`（只读老库）：导 SG+有联系方式，中文区间(「10K以上」)→代表数值，派生最强 reply_category/partnership_status |

诚实边界：①浏览器可视化本轮没跑通——预览工具被 hermes 占的 3000 端口卡住，验证靠数据通路端到端 + 公式复算，**前端样式需你刷新 5175 进达人库眼验**。②老库 `collaborations.gmv/orders` 全空、未导，1B 出单归因仍需真实店新数据。③MY 7.6 万是 scraped stub、未导。

### R14 补强（v=20260628-r15）：数据新鲜度的呈现策略——按"自信展示"收口
用户洞察："没有实时数据，TikTok API 也是定期抓的快照，数据永远是旧的"。结论：**不在浏览界面给数据贴"估算/过期"标**（=自我砸招牌，竞品也都不这么干），但也**不纯隐藏埋雷**（BD 拿死数据去谈会翻车、回头骂工具）。采用"自信展示 + 关键时刻轻提示"：
- 撤掉列表/详情/潜力分上的「估」标与详情卡 ⚠️ 估算说明——所有达人一视同仁、干净呈现（连"✓已核实"正向勋章也不加，否则反手把旧数据标成"未核实"，又绕回两级歧视）。
- 唯一新鲜度提示挪到**建联工作台「确认达人」步**：若选中达人含既往指标，出现一句中性轻提示（`.fresh-hint`）"建联前可在详情核对最新表现"——只在 BD 真要下手那刻、只为防翻车，浏览时不出现。
- 界面重心压在**不会过期的资产**：联系方式、历史建联结果、潜力分(相对排名)。战略上不跟任何人拼数据新鲜度。

---

## Round 13 — 真实店接通 + 达人库分片定期抓取　【状态：⏳ 未验收】

完成时间：2026-06-28　｜　验证：`node --check` server.js 通过；`smoke 113/0`；真实店 jasarielpicks(SG) 已授权、真实商品已拉取。

| 要验收的位置 | 做了什么 |
|---|---|
| 接入（Custom App） | 用**自定义应用·达人合作 + 本地回调** 接通真实店（绕开公共应用对外审核）；本地 `.env.local` 换成生产 app key（secret 用户填，sandbox 备份 `.env.local.sandbox.bak`） |
| 达人库 + 添加店铺 | 修了「+ 添加店铺」入口（多店铺，原来藏在平台设置）；达人库出单潜力分(Wave 1A) |
| 后端定期抓取 | `server.js` `runCreatorAutoImportOnce` 加**类目分片轮搜**（`buildCreatorShardKeywords` 用类目名做 query 分片），按 `shardIndex` 轮进，持续扩大覆盖、去重、撞限流退避。取代原"单空搜索到顶即停"。`KOL_CREATOR_AUTO_IMPORT_ENABLED=true` |

诚实边界：marketplace_creators/search 是发现接口、会到顶，分片只能做到该 API 上限，**严格"全量"得靠爬虫+时间**；新店仍受 TikTok 限流，进库速度受节流控制；分片关键词目前用类目名，待限流缓解后实测调优。

---

## Round 11 — 执行 Wave 1A：建联前「出单潜力分」（自动、零手动）　【状态：⏳ 未验收】

完成时间：2026-06-28　｜　验证：`node --check` 通过；`node smoke-test.js` 113/0。版本号 `v=20260628-r11`。

> 执行方案见 `docs/STRATEGY.md` + 计划。核心约束：**不要客户手动录入，全自动**。本轮是两根柱子里"建联前"的第一块自动基础。

| 要验收的位置 | 做了什么 |
|---|---|
| 达人库表格 | 新增 **「出单潜力分」列**（0–100，绿/蓝/灰，≥75 标「高潜」），**纯用已有数据自动算**：出单能力(GMV 35%) + 类目契合(与商家商品类目 25%) + 内容触达(粉丝/均播 20%) + 建联效率(回复率 20%)；有自有履约数据时自动纳入(15%) |
| 达人库排序 | **默认按潜力分从高到低排**（不可建联的仍排后面）→ 进达人库高潜达人自动浮顶 |
| 评分逻辑 | `creatorPotentialScore`：对数归一长尾量、字符串/范围安全解析(gmv "S$0-100"/replyRate "60%")、无商品时类目项权重自动摊回 |

须知 / 边界：
- **完全不要求客户录入**——分数全自动。鼠标悬停潜力分可看各维度拆解。
- 这是 v1 启发式估分（用户原话："只给建议、错了也没关系"）；随 1B 的履约/出单数据回流会更准。
- 真正的"产品级契合度"（达人近30天内容有没有该商品）要等 Wave 2 的 Chrome 扩展；现在的类目契合是用 TikTok 类目粗匹配。

---

## Round 10 — 弹窗关闭图标 + 市场标签美化 + 去寄样手动新增 + 达人搜索选择　【状态：⏳ 未验收】

完成时间：2026-06-28　｜　验证：`node --check` 通过；`node smoke-test.js` 113 PASS / 0 FAIL。版本号 bump 到 `v=20260628-r10`，普通刷新生效。

| # | 要验收的位置 | 做了什么 |
|---|---|---|
| 1 | docs/BACKLOG.md | 加两条待办：**寄样走 API**（核实并对齐 TikTok 寄样/履约 API）、**自动回复发图能力**（核实私信能否发图） |
| 2 | 所有弹窗右上角 | 「关闭」文字按钮 → **✕ 图标**（无边框、hover 变色，右上角） |
| 3 | 达人库「当前店铺市场 越南」 | 去掉 Win95 按钮感，改成**干净的图标式文字**（📍 当前店铺市场 越南，浅底圆角、不可点） |
| 4 | 寄样管理 | **去掉「新增寄样」手动入口**；寄样只从建联/合作流转进来。点已有寄样仍可更新状态/运单（达人/产品只读） |
| 5 | 新增/编辑合作的「达人」 | 从大下拉 → **搜索式选择**：输入用户名即时出候选（含昵称/地区），点选回填。新增可复用 `creatorPickerField` |

须知 / 边界：
- 寄样 API、自动回复发图＝**待核实，本轮不实现**（进 BACKLOG）。
- 寄样手动新增去掉后，要产生寄样请走建联记录里的「安排寄样」。
- 不碰 hermes/3000、未动 server.js。

---

## Round 9 — 徽标就地切换 + 关闭改名 + 自动回复彻底重做　【状态：⏳ 未验收】

完成时间：2026-06-27　｜　验证：`node --check` 通过；`node smoke-test.js` 113 PASS / 0 FAIL。版本号 bump 到 `v=20260627-r9`，普通刷新生效。

> 事实纠正：自动回复"自动收达人回复"**能做但没接**（已在 BACKLOG）；本轮把配置/模型一次做对，等接通即用。

| # | 要验收的位置 | 做了什么 |
|---|---|---|
| 1 | 会话顶部渠道徽标 | 点击不再弹独立框，改成**就地把对话切到该渠道**（只看该渠道消息）；前面加「全部」chip 切回，当前选中高亮 |
| 2 | 会话/详情/分组操作 | 「关闭」→**「结束跟进」**（会话）、「结束此渠道跟进」（详情单渠道）、「结束全部跟进」（分组）；逻辑不变（仍标为已关闭） |
| 3 | 自动回复配置 | **砍掉**达人类型/地区/最低粉丝 + 条件组合（规则类型只剩 包含关键词/完全匹配）。**关键词写中文**，达人当地语言来信会**先翻成中文再匹配**（回答你的核心疑问）。**回复形式＝文本/图片**：文本写中文、发送按达人语言自动翻译；图片填链接、直接发图免翻译（群发省成本）。填写指南重写讲清楚 |
| 3 | 自动回复「测试」 | 改成：模拟达人发**越南语**→系统翻成中文→用中文关键词命中规则；结果显示"来信中文 + 将回内容（文本按达人语言译/图片直接发）"，就地证明模型成立。默认测试消息已换成越南语 |
| 3 | 默认自动回复规则 | 3 条默认规则关键词改为**纯中文**（感兴趣/可以…；佣金/价格…；不需要/暂时不…） |

须知 / 边界：
- **自动回复真正触发**仍需先做「自动收达人回复」（TikTok messaging 收 + 邮箱 IMAP，已在 BACKLOG）；本轮只到"配置 + 本地测试"。
- **真发图经 TikTok 私信是否支持＝待核实 messaging API**；本轮图片只存配置（用 URL，未做上传存储）。
- **已存在的用户**（Round 8 已注入过默认自动回复）：旧规则关键词不会自动换成纯中文（含中文仍能命中），可手动编辑；**新用户**拿到纯中文默认。
- 翻译靠 MyMemory 免费额度，可能"额度已用完"（外部限制，非 bug）；不碰 hermes/3000、未动 server.js。

---

## Round 8 — 第四批验收 5 点：去手动登记 + 徽标跳转 + 状态精简 + 类目中文 + 默认模板/自动回复　【状态：⏳ 未验收】

完成时间：2026-06-27　｜　验证：`node --check` app.js/server.js 通过；`node smoke-test.js` 113 PASS / 0 FAIL。版本号 bump 到 `v=20260627-r8`，普通刷新生效。

> 事实纠正（铁律）：达人回复"自动收"**能做**——TikTok affiliate messaging 有收消息/会话能力、邮箱能接 IMAP，只是现在都没接。上轮弹窗"系统没有自动收消息接口"是错措辞。本轮按你定的：**手动登记按钮直接去掉**，自动收转正式待办。

| # | 要验收的位置 | 做了什么 |
|---|---|---|
| 1 | 会话底部 | 删掉「登记达人回复」按钮（及其弹窗/函数）。平台方视图详情里仍保留「测试·模拟达人回复」用于测状态流。自动收（TikTok messaging 收 + 邮箱 IMAP）已记入 `docs/BACKLOG.md` 正式待办 |
| 2 | 会话顶部 + 记录表渠道列 | 渠道徽标（Email/定向邀约/私信）变成**可点按钮**，点击 → 打开该渠道的「详情」卡 |
| 3 | 状态（B端） | 新增 `customerStatusLabel`，B端只暴露 **待发送/待回复/待我方处理/已成交/已关闭**；状态列、筛选下拉、会话头部都收敛到这几个（平台方视图仍看内部态全集）。触达状态里**不再泄漏 `TARGET_CO…` 原始 API 码**（失败只显示「失败」） |
| 4 | 达人库类目 | 根因＝TikTok 按店铺本地语言(越南语)返回、代码又先取了 local_name。改：`getCategories` 请求 `locale=en-US`；`normalizeCreatorCategory` 不再先返原始值、统一过中文映射；映射表补 14 个顶级类目英文→中文。**优先中文→退英文→绝不越南语** |
| 5 | 自动回复 + 消息模板 | **默认开箱即用**：内置 3 条默认自动回复（感兴趣/问佣金/婉拒）+ 5 条默认消息模板（首次建联/跟进/寄样/报价/婉拒），客户可直接用/改/加。**新增时给填写指南**：自动回复每个字段填什么有说明；模板有 `{KOL名称}/{产品名称}/{联盟佣金率}` 变量**点击插入**到光标处 |

须知 / 边界：
- **#4 需重启 8015 后端 + 重新同步达人**才生效（类目是后端拉取时归一化的）；**已存在的达人记录里存的越南语类目，要重新同步才会刷新**——不重启/不重新拉的话旧数据仍是越南语。要我顺手重启 8015 你说一声。
- **#5 默认模板/自动回复**：通过一次性 `defaultsSeededVersion` 注入，老用户（模板/自动回复为空）刷新即获得默认；已自建过的不覆盖。
- 自动收达人回复**本轮不实现**（转待办）；不碰 hermes/3000；真实发送受 sandbox 限制。
- 类目 `locale` 字段按 TikTok 文档先试 `en-US`，文档是 JS 渲染没 100% 核到字段名；实测若仍越南语，退回按 category_id 纯自维护映射。

---

## Round 7 — 第三批验收 7 点：按达人归纳 + 翻译进正文 + 操作收纳　【状态：⏳ 未验收】

完成时间：2026-06-27　｜　验证：`node --check` app.js/server.js 通过；`node smoke-test.js` 113 PASS / 0 FAIL。缓存版本号已 bump 到 `v=20260627-r7`，普通刷新即生效。

> 本轮决策（plan 模式确认）：① 记录/会话**按达人归纳成一条**（渠道作标签，跨批次也并）；② **译文作为正文**，另给「查看中文原文」+ 达人回复「翻译成中文」；③ 操作列收成「更多▾」下拉。

| # | 要验收的位置 | 做了什么 |
|---|---|---|
| 1 | 建联记录表 | 同一个达人的多渠道/多批次**归纳成一行**（渠道列多 badge、状态取汇总、触达状态聚合各渠道），不再每渠道一行 |
| 2 | 沟通会话 | 同一个达人**一个会话**，左侧列表去重到达人，右侧线程合并该达人全部渠道消息按时间排序、每条带渠道标签 |
| 3 | 建联详情弹窗 | 点「详情」列出该达人**每条渠道**（渠道+状态+产品+最后消息），每条各自的提交/标记发送失败/查看结果/测试推进按钮——动作绑定到具体渠道，不再作用在隐藏重复记录上 |
| 4 | 会话底部「登记达人回复」 | `prompt` 改成**弹窗**：带说明（系统无自动收消息接口，达人回复需手动登记）+ 选来自渠道 + 文本框 |
| 5/6 | 会话气泡 + 翻译 | **我方气泡正文＝译文**（达人能看懂），下面「查看中文原文」折叠按钮给 BD 看实际发了什么；**达人回复气泡**加「翻译成中文」按钮（调 /api/translate）。`saveOutreach` 把译文也写进定向邀约记录 |
| 7 | 建联记录操作列 | 行内只留 **查看沟通 + 更多▾** 下拉（详情/提交全部待发送/关闭全部/不感兴趣/删除全部），行高大幅压缩 |

须知 / 边界：
- **译文进正文只对「本次改动后新发起的建联」生效**：之前已存在的记录，会话首条气泡是历史存的中文（不回溯改写）。要验收 5/6 请**新发起一条建联**（写中文→翻译→提交）再看会话。
- **翻译依赖 MyMemory 免费额度**：今天额度可能已用尽（会提示「额度已用完」），属外部限制非 bug；换天或设 `MYMEMORY_EMAIL` 可恢复。
- 数据模型未改（仍每渠道一条 record，只是展示层归纳）；真实 TikTok 私信/邀约发送仍受 sandbox 限制。
- 本轮未能在助手内置浏览器预览里截图：预览工具启动时与你的 **hermes（端口 3000）** 冲突，我没去动 hermes，请在已运行的 5175 上自行走查。

---

## Round 1 — 一致性 + 建联视觉/流程对齐　【状态：⏳ 未验收】

完成时间：2026-06-24　｜　验证：`node --check` 三文件通过；`node smoke-test.js` 113 PASS / 0 FAIL

| 要验收的位置 | 做了什么处理 |
|---|---|
| `DESIGN.md` / `PRODUCT.md` | sidebar token 改白(对齐实现)；保持单商家多店铺、不引入服务商；删 WhatsApp；补达人类型/禁止标签/客户侧约束 |
| 全站（KOL池筛选、KOL详情、KOL编辑、消息模板、平台设置、系统消息、CSV导入、回复弹窗） | WhatsApp 全清：app.js 24处→0；server.js 删 2 处死字段 |
| 产品管理「连接状态」、系统消息「同步日志」 | 技术词清理：后端已配置/后端未启动/API后端 → 服务已就绪/服务未连接/连接服务 |
| 建联记录页 | 状态显示走业务词映射（内部值保留，不裸露 API 字样） |
| **发起建联 workbench**（商品与佣金 / 建联动作 / 样品） | 商品加可选/不可选态；样品改两段式(是否提供×审核方式)；渠道改主次(定向邀约主/私信跟进/Email备用) |

未做（推迟到 Round 2）：邀约卡、商品可推广真实 API 标志、后端 payload 调整、server.js seller_contact_info 的 whatsapp 残留。

---

## Round 2 — 邀约卡（前端）　【状态：⏳ 未验收】

完成时间：2026-06-24　｜　验证：`node --check` 通过；`node smoke-test.js` 113 PASS / 0 FAIL

| 要验收的位置 | 做了什么处理 |
|---|---|
| 发起建联 → 定向邀约面板 | 新增「随定向邀约发送邀约卡」开关 + **实时邀约卡预览**（选中商品 · 标准/广告佣金 · 样品规则，随勾选/佣金变化更新） |
| 提交建联后的私信/Email 文案 | 开关开启时，把邀约卡摘要拼进真实会发的私信/Email 文案 |
| 建联记录 → 触达状态列 | 携带邀约卡的记录显示「邀约卡 · 已附带」 |

诚实边界（按"不假设 TikTok API"铁律，本轮**不做**，待查后再定）：
- **真实 IM 富文本邀约卡发送**：TikTok 私信 API 是否支持附带富卡未证实——本轮邀约卡=配置+摘要注入文案，不写假装发卡的后端。
- **server.js `seller_contact_info.whatsapp/telegram/line`**：从不被填充、永远空串；整字段从 TikTok payload 移除需先确认 schema 是否接受缺省子字段。
- **商品"可推广"真实标志**：现按本地 status 判断不可选；接真实标志需确认 TikTok 商品 API 是否返回该字段。
- 邀约卡内容拖拽排序（邀约卡/文本/图片）：价值低，跳过。

---

## Round 3 — 沟通/会话线程视图　【状态：⏳ 未验收】

完成时间：2026-06-24　｜　验证：`node --check app.js` 通过；`node smoke-test.js` 113 PASS / 0 FAIL

| 要验收的位置 | 做了什么处理 |
|---|---|
| 建联记录 → 每行「查看沟通」 | 不再跳 KOL 详情，改为打开**沟通会话页**（新页 `#conversation`，不进侧边栏，从建联记录进入） |
| 沟通会话页（双栏） | 左=会话列表（全部建联记录，可切换）；右=消息线程（气泡：我方/达人/系统）+ 头部状态与动作 |
| 会话页底部回复编辑区 | 选渠道 + 写回复 → 发送，回复记入会话线程；建联记录原弹窗回复也同步入线程 |
| 「记录达人回复」按钮 | BD 手动录入达人真实回复内容，入线程并把状态推进到"待我方回复"（无真实入站 API，不造假自动收消息） |
| 会话头部动作 | 标记已回复 / 安排寄样 / 进入合作 / 关闭（复用既有流转） |

诚实边界（按"不假设 TikTok API"，本轮**不做**）：
- **真实发送/接收**：会话里的回复与达人消息是本地记录；真正经 TikTok 私信/Email 收发仍依赖接入（sandbox 配额限制，无法真验）。
- 截图：3000 端口被你另一个项目的 hermes WhatsApp bridge 占用，我没擅自杀；**这页的视觉需要你在 5175 看，或授权我关掉 hermes 后我来截图**。

---

## Round 6 — 第二轮验收 11 点 + 缓存根治 + 角色隔离　【状态：⏳ 未验收】

完成时间：2026-06-24　｜　验证：`node --check` app.js/server.js 通过；`node smoke-test.js` 113/0

**关键：缓存真凶** — index.html 的 `?v=` 版本号从没改过，导致 R1–R5 改动一直没加载（被当成"没生效"）。已 bump 到 `v=20260624-r7`，并写进 CLAUDE.md「改前端必 bump 版本号」。**现在普通刷新即可，不用强刷。**

| # | 要验收的位置 | 做了什么 |
|---|---|---|
| 0 | 全站 | 缓存版本号 bump，R1–R6 全部改动普通刷新即生效 |
| 1 | 达人库 | 删风险提示卡；加「全量达人库 / 有联系方式」两视图 tab |
| 2 | 发起建联商品表 | 商品图裂→onerror 兜底首字母；广告佣金"可选"→"选填" |
| 3 | 发起建联邀约卡 | 改成 Proboost 风格商品卡（缩略图+名称+佣金） |
| 4/5 | 翻译/提交提示框 | 之前是缓存；缓存修好后已是 R5 的清空译文 + toast |
| 6 | 建联记录 | 产品/下一步/最后消息列**截断**(2行)，加「详情」弹窗看全文 |
| 7 | 建联记录（仅平台方视图） | 测试按钮：标记已发送/模拟达人回复/进合作，可走完状态流 |
| 8 | KOL详情沟通 + 会话页 | 去掉每条回复按钮和"选平台"弹窗；消息按来源标私信/邮箱；回复框自动渠道+翻译；KOL详情→「进入会话回复」 |
| 9 | KOL详情/编辑/导入 | 恢复 WhatsApp 联系方式展示（非发送渠道） |
| 10 | 顶栏/系统消息 | 系统消息→右上角铃铛(未读数)；删同步日志；侧栏移除 |
| 11 | 顶栏/侧栏 | 角色隔离：viewRole(平台方/B端)切换；B端隐藏平台管理端 |

须知：**普通刷新 5175 即可**（版本号已变）。角色隔离默认 B端视图（看不到平台管理端）；切「平台方视图」看全部 + 测试按钮。

---

## Round 5 — 验收反馈修复 + 静默自启　【状态：⏳ 未验收】

完成时间：2026-06-24　｜　验证：`node --check` 通过；`node smoke-test.js` 113 PASS / 0 FAIL；看门狗实测（kill 5175 → 自动拉回）

用户在 5175 验收时反馈的问题，逐条修复：

| 要验收的位置 | 做了什么处理 |
|---|---|
| 达人库列表 | ③ 冷却 bug：**失败/测试的建联不再锁 24 小时**（只有真发出成功的才冷却）；可建联的排前面、不可建联的排后面 |
| KOL详情沟通卡 + 会话气泡 | ④ 刺眼亮蓝 `#1f3faa` → **柔和浅蓝底+深字** |
| KOL详情沟通区 | ⑤ **限高可滚动**、卡片不撑爆页；左侧达人卡 **sticky 常驻可见** |
| 发起建联 → 翻译目标语言 | ⑥ **切换语言时自动清空旧译文**（不再残留越南语） |
| 建联记录 → 提交全部待发送 | A 原生 alert → **系统样式 toast**（右下角自动消失） |
| 回复弹窗 + 会话页回复框 | B 加「**翻译成达人语言**」按钮（调 /api/translate） |
| 邮箱配置弹窗 | C 「邮箱类型」**真正可选生效**：切 Gmail/Outlook/其他 自动填 SMTP/IMAP + 更新指南（顺手修了 `<\b>` 老 typo） |
| 系统稳定性（系统层） | D **静默无窗口开机自启**（启动文件夹 `KOL_Compass_Autostart.vbs`）+ **看门狗计划任务**（每 3 分钟检查，掉了静默拉回）。误关/崩溃 3 分钟内自愈 |

须知：A/B/C 前端改动**刷新 5175（Ctrl+F5）即生效**；C 的 SMTP/IMAP 实发仍需重启 8015（已重启）。开机自启需**你重启电脑**确认无窗口自起。

---

## Round 4 — 真翻译接入（MyMemory 免费 API）　【状态：⏳ 未验收】

完成时间：2026-06-24　｜　验证：`node --check` 通过；`node smoke-test.js` 113 PASS / 0 FAIL；直连 MyMemory 实测 zh→越南语/泰语返回真实流畅译文

| 要验收的位置 | 做了什么处理 |
|---|---|
| 发起建联 → 消息与翻译 →「翻译成达人语言」按钮 | 不再吐写死套话；改为**真翻译 BD 实际写的中文文案**（先渲染占位符），结果填进可编辑「翻译预览」框，带"翻译中…"和失败提示 |
| 后端 server.js `/api/translate` | 新端点调 **MyMemory 免费 API**（无 key、无绑卡）；语言映射 中文→zh-CN / 英语en / 越南语vi / 泰语th / 马来语ms / 印尼语id / 菲律宾语tl；额度用尽/同语言/不可达都有明确报错 |

风险 / 须知：
- **需重启 8015 后端**（start-full.bat / start-api-8015.bat）才能加载新 `/api/translate` 路由——你现在跑着的是旧进程。
- MyMemory 免费额度：匿名约 5000 字/天；设环境变量 `MYMEMORY_EMAIL=你的邮箱` 可提到约 5 万字/天。
- 单条过长（约 >500 字）可能失败；建联文案通常远低于此。量真大了再切自建 LibreTranslate（已记后路）。
- 批量建联（saveOutreach 多达人自动填充）仍用旧的占位草稿，未逐条真翻译——避免提交时循环打外部 API。**交互式「翻译」按钮是真翻译**，这是建联编辑主路径。

---

---

## Round 29 — 11项实测反馈·第一批两个 bug（头像 + 去重）　【状态：⏳ 未验收】

完成时间：2026-06-29　｜　版本 `v=20260629-r29`　｜　验证：`node --check` server.js / app.js 通过；后端已重启健康；头像端点实测 200 image/webp；去重后 0 重复

| 要验收的位置 | 做了什么处理 |
|---|---|
| 达人库 / 沟通页 达人头像（#10 全裂） | 根因=头像按**前端重排 id**（1,2,3…）找，跨刷新错位→全 404。改为按 **username** 匹配：前端 `creatorAvatarSrc` 传 `c.username`；后端 `/api/avatar` 按 username（大小写不敏感）找达人、缓存文件名也用 username。实测 `enreview2`/`ENReview2` 都命中同一张图 |
| 达人库总数（#11 重复 14 个） | 根因=去重**区分大小写**（老库 `IamJamieYeo` vs 平台 `iamjamieyeo` 没合并）。改 `platformCreatorKeys` username 统一转小写；并跑一次性合并脚本把已存在的 14 组重复合并（保留新鲜 TikTok 指标 + 并联系方式/建联历史），库 **2616 → 2602，0 重复**。原文件备份在 `.data/platform-creators.json.bak-dedup` |

**怎么验收（你操作）：**
1. 达人库 **Ctrl+F5** 强刷 → 头像应该普遍出图（不再是首字母占位/裂图）。
2. 达人库总数应显示 **新加坡市场约 2189**（全量库 2602）；不应再看到同名达人两行。

须知：旧的以数字 id 命名的头像缓存文件（如 `8304330.webp`）成为孤儿、不再被引用，无害；新缓存按 username 落盘。

---

## Round 30 — 11项实测反馈·主体（带货信号 + 审查流程 + 评分统一 + 相似达人）　【状态：⏳ 未验收】

完成时间：2026-06-29　｜　版本 `v=20260629-r30`　｜　验证：6 个 JS 文件 `node --check` 全过；后端重启健康；带货信号 ingest 端到端落库实测通过（ecVideoCount/ecVideoRatio 正确持久化）

承上：R29 已交付 #10 头像 / #11 去重 / #1 删按钮 / #4 自动采集默认开。本轮交付其余主体：

| # | 你提的问题 | 做了什么 | 怎么验收 |
|---|-----------|---------|---------|
| **#5** | 评论率不可靠，能不能看带货数量 | 撤掉「评论买家意向」；扩展从 `item_list` 抓**带货视频标记**（anchors/商品锚点/commerce），算「近期带货视频数 / 占比」→ 回流入库 → 进评分与详情。⚠️ **带货字段未真机定论**，宽口径抓+留诊断（`_anchorTypesSeen`），验收时真机核对 | 见下「#5 真机核对」 |
| **#2/#7/#9** | 评论率信息不稳、只弹第一个视频 | 随 #5 一并**移除评论意向**（弹窗灯、详情卡、评分项全部换成带货）| 弹窗不再有「买家意向」，改「带货经验」 |
| **#3** | Compass 契合高、打开弹窗又说不建议（口径不一致）| 评分统一：**内容实证不发你品类→契合分直接压到 12**（原来只给 25），和弹窗红灯一个口径；潜力分会明显被拉低 | 选一个明显不发你品类的达人，Compass 潜力分应偏低、弹窗也红 |
| **#4/#8** | BD 流程不对，应「选好达人→审查→跳 TikTok→评分」| KOL 详情页加**「审查达人 ↗」按钮**：一键打开其 TikTok 主页 → 采集器自动体检 → 回流更新分。BD 只点一个按钮 | 见下「#4/#8 流程」 |
| **#6** | 增加相似达人按钮 | KOL 详情页加**「相似达人」按钮**：按该达人内容品类在库内筛同类目达人（官方"找相似"需服务商权限，先用库内兜底）| 点「相似达人」→ 跳达人库并按其品类筛选 |

### 验收流程（你怎么操作）

**前置**：① 浏览器开 `http://127.0.0.1:5175` 并 **Ctrl+F5** 强刷；② `chrome://extensions` 里点 Compass 采集器的**「重新加载」**（扩展改了不重载不生效）。

**#4/#8 审查流程**：达人库点任一达人进详情 → 点右上**「审查达人 ↗」** → 新标签打开其 TikTok 主页 → 在该页**往下滚一屏**让视频墙加载（采集器自动体检，限速 60s/人）→ 回到 Compass 详情页 **F5** → 「建联前评估」里带货视频数/互动率应按最新内容更新。

**#5 真机核对（关键）**：开一个**你确定在带货**的达人 TikTok 主页 → 点扩展图标看「带货经验」灯：
- 若显示「N/M 条带货」且 N 合理（这人确实挂了商品）→ ✅ 字段抓对了。
- 若显示「近期未见带货」但他明明在带货 → ❌ 字段没命中，**告诉我**，我加诊断 dump 看 `anchorTypes` 真实结构再校准（这步我没真机确认过，按规矩不假装准）。

**#6 相似达人**：详情页点「相似达人」→ 应跳到达人库、品类筛选已设为该达人的内容品类、列表是同类目达人。

**#3 评分统一**：找一个内容明显跟你店铺品类不搭的达人 → Compass 潜力分应偏低（契合项被压到 12）+ 打开扩展弹窗内容契合应红灯——两边结论一致。

须知：扩展改了 4 个文件（inject/content/popup + 默认开），**必须在 chrome://extensions 重新加载**。带货字段真机核对是 #5 能不能上生产的前提。

---

## Round 31-32 — 前端残留重复修复 + 相似达人加权排序　【状态：⏳ 未验收】

完成时间：2026-06-29　｜　版本 `v=20260629-r32`　｜　验证：`node --check` 通过；后端库实测 0 重复（xianexy 仅 1 行）

| 位置 | 做了什么 | 怎么验收 |
|---|---|---|
| 达人库重复（R29 后仍见 @xianexy 两行）| 根因=**前端 localStorage 残留**后端去重前的旧副本，合并不删已有行。改 `creatorImportKeys` username 转小写；新增 `dedupeStateCreators()` 在加载达人库时按小写 username 合并 state.creators（保留资料最全+并联系方式/历史/recentPerf）| **Ctrl+F5** 后达人库不应再有同名两行（后端已 0 重复，前端刷新即收掉残留）|
| 相似达人（升级）| 从"只按类目粗筛"升级为**加权排序**：`creatorSimilarity` = 类目重合(最多90) + 粉丝量级相近(最多25) + 带货度相近(最多15)，不同类目直接剔除。详情页点「相似达人」→ 达人库按相似度排序+顶部横幅（可「返回全部达人」）| 详情页点相似达人 → 列表是同类目、粉丝量级相近的，顶部有横幅 |

须知：相似达人官方"找相似"需服务商权限，当前为库内加权兜底；插件侧不做相似（结论：相似排序留系统）。

---

## Round 33 — 老库"用户名/昵称写反"导致的重复+错链+裂图　【状态：⏳ 未验收】

完成时间：2026-06-29　｜　版本 `v=20260629-r33`　｜　验证：后端合并 35 条孤儿（2610→2575）；前端跨合并逻辑隔离单测通过；AnnaYoYoYo 复核为单行正确 handle

**用户发现**：@AnnaYoYoYo 第一个就"找不到达人、没信息"。
**根因**：老库导入里有 35 条记录把**人名填进了 username、真 TikTok handle 填进了 nickname**（如 username=AnnaYoYoYo / nickname=annayeo44）。这些 handle 平台库里有正确行，但因 username 字符串不同没合并 → 重复 + TikTok 链接指向假 handle（404）+ 头像裂。（其余 815 条纯老库联系人 username 基本都是合法 handle，不受影响。）

| 做了什么 | 怎么验收 |
|---|---|
| 后端一次性合并：35 条孤儿的联系方式/建联历史并入平台孪生（正确 handle+头像+sourceId），删除孤儿。库 2610→2575（备份 `.bak-swapfix`）| 后端已修，无需操作 |
| 前端 `dedupeStateCreators` 加第二遍**交叉合并**：localStorage 残留的孤儿（nickname 命中某平台行 username）也会被并掉，防止你刷新后旧副本还在 | **Ctrl+F5** 后 @AnnaYoYoYo 应只剩一行、显示 @annayeo44、有头像、TikTok 链接能打开、带 email/WA |

须知：纯前端修复，Ctrl+F5 即生效；后端数据已清理。

---

## Round 34 — 出单潜力分透明化（算法说明 + 合作档位）　【状态：⏳ 未验收】

完成时间：2026-06-29　｜　版本 `v=20260629-r34`　｜　验证：`node --check` 通过

用户拍板：档位 **≥65 建议建联 / 40–64 可考虑 / <40 谨慎**；展示=**分数旁问号 + 点开说明**。

| 位置 | 做了什么 | 怎么验收 |
|---|---|---|
| 达人库「出单潜力分」表头 | 加 **? 问号**，点开弹窗讲清算法（6 维度+权重）和合作档位 | 点表头 ? → 弹出说明 |
| 每行分数 | pill 旁加**档位标签**（建议建联/可考虑/谨慎），颜色：绿/蓝/灰；按分排序 | 列表每行分数旁有档位词 |
| KOL 详情「建联前评估」 | 分数旁同样加档位标签 + ? 说明入口 | 详情页评估卡看到档位+问号 |
| 档位阈值 | 全系统统一用 `scoreTier()`：65/40 | 同一达人在库列表/详情档位一致 |

须知：纯前端，**Ctrl+F5** 即生效。档位是"潜力"参考非保证；分数悬浮可看各维度拆解（保留）。

---

## Round 35-38 — 建联前体检优化批次　【状态：⏳ 未验收】

完成时间：2026-06-29　｜　版本 `v=20260629-r38`（前端）｜ 验证：`node --check` 全过；评分分布实跑确认档位仍成立

| 编号 | 做了什么 | 怎么验收 |
|---|---|---|
| 搜索栏失焦 | `render()` 记住焦点+光标重渲染后还原；6 个搜索框加稳定 id | 达人库搜索框连续打字不再打一个字就停 |
| 翻译换平台 | `/api/translate` 改 `TRANSLATE_PROVIDER` 可插拔（mymemory默认/google/deepl），上正式设环境变量+key即切，不改代码 | MyMemory 默认仍正常（已实测中→越南语）；google/deepl 未实测，上正式填 key 后验 |
| 带货识别偏少(8/15) | 放宽：达人页任意商品锚点即记带货；改名「带货经验」→「带货频率」；加 `[KC带货诊断]` 控制台日志；修 popup 残留 captureBtn 报错 | 重载扩展+刷新 → 带货数接近实际；偏少则发我控制台诊断行 |
| 回复率拖累全库 | 回复率未知（全库100%是"-"）时该项剔除，权重摊回 GMV/类目/触达 | 分数普遍回升；档位 65/40 经分布验证不动 |
| 互动率阈值 | 绿灯 3%→2.5%（popup+面板） | 带货达人不再被误判偏低 |
| 品类匹配「抓接近」 | 不补全量映射表；改字符相似度模糊匹配 `categorySimilarity`（去连接词/通用后缀+Dice），处理命名变体(健康/健康保健、家电/家用电器、居家日用/家居日用…)+分号拼接串 | 契合更准；同类1473/接近346/不契合760；零误判已测 |

暂缓（用户定）：黑名单补全（详情页拉黑/不感兴趣 + prompt换弹窗 + 批量拉黑 + 会话内拉黑）——慢慢做，已记。

---

## Round 39 — 产品级内容匹配（不再只比 TikTok 大类目）　【状态：⏳ 未验收】

完成时间：2026-06-29　｜　版本 `v=20260629-r39`（前端）｜ 验证：`node --check` 全过；关键词管线模拟测通过（带相关货达人命中、美妆达人不命中）

用户澄清的真需求：插件标"内容契合"要**贴近客户的具体产品**，不是笼统的 TikTok 大类目。

| 改动处 | 做了什么 |
|---|---|
| 后端 `/api/merchant/context` | 除 categories 外，新存 **productNames + productKeywords**（GET 一并返回） |
| 前端 `syncProducts` | 同步商品时提炼产品关键词（`productNamesToKeywords`：去营销噪声词，英文 token + 中文整词/双字）推给后端 |
| 扩展 `inject.js` | 钩 item_list 时多抓 **anchorKeywords**（商品锚点里的商品名 = 达人在带什么货） |
| 扩展 `content.js`/`popup.js` | 内容契合改**产品级**：达人在带的商品名 + 文案 + 话题 vs 客户产品关键词 → 命中显示「贴近你的产品：xxx」；不沾→红灯；**无产品关键词时退回大类目**（兜底不变） |
| 后端 ingest | recentPerf 持久化 ecProductNames（供 Compass 主站日后用） |

**关键前置**：merchant-context.json 现在只有 categories，**产品关键词要等你在 Compass 里同步一次商品**才会推上去。

### 验收流程
1. Compass **Ctrl+F5** → 进**产品管理 → 同步商品**（这步把产品关键词推给后端）。
2. `chrome://extensions` **重载扩展**。
3. 开一个达人 TikTok 页 → 面板「内容契合」应显示**产品级**结论（「贴近你的产品：water、cup…」或「内容跟你的产品不沾」），不再是「命中家用电器」这种大类目。
4. 库那 2577 个没体检的达人仍走大类目兜底（没体检就没内容数据，无解）。

---

## Round 41 — 建联前核验向导（逐个）+ 产品级精准打分　【状态：⏳ 未验收】

完成时间：2026-06-30　｜　版本 `v=20260629-r41`（前端）｜ 验证：`node --check` 通过

| 改动 | 做了什么 |
|---|---|
| 一键建联入口 | 勾选 ≥2 达人点「一键建联」→ 先弹**核验说明框**（解释核验=打开主页抓真实内容、跟产品精确匹配重打分、比粗略分准）→ [逐个核验] / [跳过直接建联] |
| 逐个核验向导 | 进度 N/总、当前达人体检卡 + 产品命中行；「打开 TikTok 核验」(开 TA 主页，采集器自动体检) + 「刷新体检」看更新分；「✓加入建联 / ✗跳过」逐个走；走完只把勾✓的带进建联 |
| 产品级精准打分（app.js 评分） | 核验后达人有真实内容时，`creatorProductFitMatches` 用 TA 带货商品名+文案+话题 vs 你的产品关键词精确匹配 → 命中即契合满分（替代只看大类目的粗略契合）；进 `creatorCategoryFitScore`→潜力分 |

### 验收流程
1. Compass **Ctrl+F5**，先**产品管理→同步商品**（让产品关键词就绪）。
2. 达人库勾选 ≥2 个达人 → **一键建联** → 应弹核验说明框。
3. 点「逐个核验」→ 向导出现 → 「打开 TikTok 核验」开达人主页 → 回 Compass 点「刷新体检」→ 分数/产品命中应更新得更准 → ✓/✗ 逐个 → 走完进建联（只含✓的）。

须知：每开一个达人页靠点击触发（浏览器限制），即"点一下开一个、看完勾、自动下一个"，符合"看一个勾一个"。
